{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip3 install -r api/requirements.txt"
  },
  "deploy": {
    "startCommand": "cd api && gunicorn main:app",
    "restartPolicyType": "ON_FAILURE"
  }
}
